# gwt - Git Worktree TUI Manager

> 멀티 브랜치 동시 작업을 위한 Cockpit  
> Bare repository 기반 Git worktree 관리 TUI

---

## 1. 프로젝트 개요

### 1.1 배경 및 문제 정의

Git worktree는 하나의 repository에서 여러 브랜치를 동시에 체크아웃할 수 있는 강력한 기능이다. 특히 bare repository와 조합하면 다음과 같은 워크플로우가 가능해진다:

```
project.git/              # bare repository (메타데이터만)
├── main/                 # main 브랜치 worktree
├── feature-auth/         # feature 브랜치 worktree
└── hotfix-payment/       # hotfix 브랜치 worktree
```

**현재 문제점:**

| 문제 | 현재 상황 |
|------|----------|
| 복잡한 CLI | `git worktree add ../feat-x -b feat-x origin/main` 매번 타이핑 |
| 상태 파악 어려움 | 각 worktree마다 `cd` + `git status` 반복 |
| 경로 관리 | worktree 경로를 기억해야 함 |
| 정리 누락 | 완료된 worktree 삭제 잊어버림 |
| AI 코딩 환경 | Claude Code 등 터미널 기반 AI 도구 사용 시 브랜치 전환이 작업 흐름을 끊음 |

**Claude Code 워크플로우 Pain Point:**

```
┌─────────────────────────────────────────────────────────────────┐
│  Terminal 1: claude code (main에서 작업)                         │
│  Terminal 2: claude code (feature-a에서 작업)                    │
│  Terminal 3: 테스트/빌드                                         │
│                                                                 │
│  문제:                                                           │
│  - "지금 어느 브랜치에서 뭐하고 있었지?"                            │
│  - "새 worktree 만들어야 하는데 명령어가..."                       │
│  - "이 터미널이 어느 worktree였지?"                               │
│  - "hotfix 급하게 해야 하는데 어디서 시작하지?"                     │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 목표

**핵심 가치:** 멀티 브랜치 동시 작업의 cognitive load를 최소화

- 한 화면에서 모든 worktree 현황 파악 (Dashboard)
- 키 몇 번으로 worktree 생성/삭제/이동
- 직관적인 TUI로 CLI 명령어 암기 불필요
- Bare repo + worktree 패턴을 first-class로 지원

### 1.3 타겟 사용자

- **Primary:** Claude Code, Cursor 등 AI 코딩 도구와 함께 멀티 브랜치 작업하는 개발자
- 여러 feature 브랜치를 동시에 작업하는 개발자
- hotfix/release 브랜치를 빈번하게 다루는 개발자
- bare repository + worktree 패턴을 사용하거나 도입하려는 팀

### 1.4 기존 도구와의 차별점

| 도구 | 포지셔닝 | 한계 |
|------|----------|------|
| **lazygit** | git 작업 전체를 TUI로 | worktree는 부가 기능, 일반 repo 기준 설계 |
| **git-worktree-switcher** | worktree 전환에 집중 | 상태 표시 없음, 생성/삭제는 CLI |
| **git-worktree.nvim** | Neovim 내 전환 | 에디터 종속, 전체 현황 파악 불가 |

**gwt 포지셔닝:**

```
lazygit  = "git 작업 전체를 TUI로"
gwt      = "멀티 브랜치 동시 작업을 위한 Cockpit"
```

| 차별점 | 설명 |
|--------|------|
| **Bare Repo First** | bare + worktree 패턴이 기본 전제, 일반 repo도 지원 |
| **Project Dashboard** | 모든 worktree 상태를 한눈에, ahead/behind 표시 |
| **Batch Operations** | 전체 fetch, 다중 worktree 동시 관리 |
| **AI Coding 친화적** | 터미널 기반 워크플로우에 최적화 |

---

## 2. 기능 범위

### 2.1 MVP (v0.1)

| 기능 | 설명 | 우선순위 |
|------|------|----------|
| **List** | 모든 worktree 목록 + git status 요약 | P0 |
| **Add** | 새 worktree 생성 (새 브랜치 / 기존 브랜치) | P0 |
| **Remove** | worktree 삭제 (확인 다이얼로그) | P0 |
| **Open Editor** | 선택한 worktree를 `$EDITOR`로 열기 | P0 |
| **Open Terminal** | 선택한 worktree 경로에서 새 터미널 열기 | P0 |
| **Fetch** | 전체 remote fetch | P1 |

### 2.2 Bare Repo First (v0.2)

> 일반 도구들이 일반 repo 기준인 것과 달리, bare repo + worktree를 first-class로 지원

| 기능 | 설명 |
|------|------|
| **Init Wizard** | 일반 repo → bare + worktree 전환 가이드 |
| **Clone Bare** | `gwt clone <url>` → bare repo + main worktree 한번에 생성 |
| **Path Convention** | worktree 경로 자동 생성 규칙 (설정 가능) |
| **First Run Setup** | bare repo 아니면 전환 여부 안내 |
| **Branch Cleanup** | worktree 삭제 시 로컬 브랜치 동시 삭제 옵션 |

**Path Convention 예시:**

```
# 설정 가능한 패턴
~/repos/{project}.git/          # bare repo
~/repos/{project}/{branch}/     # worktree (기본)
~/repos/{project}-{branch}/     # worktree (flat)

# 자동 생성 예시
gwt add feature/auth
→ ~/repos/myproject/feature-auth/
```

### 2.3 Project Dashboard (v0.3)

> 모든 worktree를 한눈에 보고 동시에 관리하는 Cockpit

| 기능 | 설명 |
|------|------|
| **Batch Status** | 모든 worktree 상태 한번에 갱신 (비동기) |
| **Batch Fetch** | 전체 fetch + 각 worktree ahead/behind 표시 |
| **Branch Compare** | worktree 간 커밋 차이 시각화 (`↑3 ↓1` 형태) |
| **Quick Switch** | fuzzy finder로 worktree 검색 + 즉시 이동 |
| **Session Resume** | 마지막 작업 worktree 기억 |
| **Split Pane UI** | 좌측 리스트 + 우측 상세/Overview 패널 |
| **Detail View** | 선택한 worktree의 staged, unstaged, recent commits |

**Dashboard UI 목표:**

```
┌─────────────────────────────────────────────────────────────────────────┐
│  gwt v0.3                                        bare: ~/repos/proj.git │
├─────────────────────────────────────────────────────────────────────────┤
│                                         │                               │
│  WORKTREES                              │  OVERVIEW                     │
│  ─────────────────────────────────────  │  ─────────────────────────    │
│                                         │                               │
│  ▸ main           ✓ clean    ↓2        │  Total: 4 worktrees           │
│    feature/auth   ● staged   ↑3 ↓1     │  Dirty: 2                     │
│    feature/dash   ✗ dirty    ↑5        │  Behind remote: 2             │
│    hotfix/bug     ✓ clean    ↑1        │                               │
│                                         │  Last fetch: 5m ago           │
│                                         │                               │
│                                         │  QUICK ACTIONS                │
│                                         │  ─────────────────────────    │
│                                         │  [F] Fetch all                │
│                                         │  [S] Sync current             │
│                                         │  [N] New hotfix               │
│                                         │  [/] Search worktree          │
│                                         │                               │
├─────────────────────────────────────────────────────────────────────────┤
│  feature/auth: ↑3 ahead, ↓1 behind origin │ Last: "Add OAuth" (5m ago)  │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.4 External Integration (v0.4+)

| 기능 | 설명 |
|------|------|
| **PR Status** | GitHub/GitLab API 연동, PR 상태 표시 |
| **Worktree Template** | `hotfix/*` → `hotfix/` 디렉토리, `feature/*` → `feature/` 자동 |
| **Project Config** | `.gwt.toml` 팀 공유 설정 파일 |
| **CI Status** | GitHub Actions / GitLab CI 상태 표시 |
| **Multiple Repos** | 여러 bare repo 동시 관리 |

### 2.5 기능 로드맵 요약

```
v0.1 (2주)     MVP - 기본 CRUD
    │          List, Add, Remove, Open, Fetch
    │
    ▼
v0.2 (2주)     Bare Repo First
    │          Init wizard, Clone bare, Path convention
    │
    ▼
v0.3 (3주)     Project Dashboard
    │          Batch ops, Ahead/behind, Fuzzy search, Split pane
    │
    ▼
v0.4+          External Integration
               PR status, Team config, CI status
```

---

## 3. 사용자 시나리오

### 3.1 새 feature 브랜치 작업 시작

**Before (CLI):**
```bash
cd ~/repos/project.git
git worktree add ../feature-auth -b feature-auth origin/main
cd ../feature-auth
code .
```

**After (gwt):**
```
1. gwt 실행
2. 'a' 키로 Add modal 열기
3. "feature-auth" 입력 → Enter
4. 자동으로 ~/repos/feature-auth 생성
5. 'o' 키로 에디터 열기
```

### 3.2 긴급 hotfix 대응

**상황:** feature 작업 중 production 버그 발견

**Before:**
```bash
# 현재 작업 stash하거나 commit
git stash
cd ../main
git pull
git worktree add ../hotfix-bug -b hotfix-bug
cd ../hotfix-bug
# ... 수정 후
cd ../feature-auth
git stash pop
```

**After:**
```
1. gwt에서 현재 worktree 상태 확인 (dirty면 경고)
2. 'a' → "hotfix-bug" → main base 선택 → Enter
3. 새 worktree에서 바로 작업
4. 완료 후 'd'로 삭제
```

### 3.3 현재 상태 파악

**Before:**
```bash
git worktree list
cd ../main && git status
cd ../feature-auth && git status
cd ../hotfix-bug && git status
```

**After:**
```
gwt 실행 → 한 화면에 전체 현황 표시
```

---

## 4. User Flow

### 4.1 Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│    Start ─────► Bare repo? ─────► No ──► Error & Exit           │
│                    │                                            │
│                   Yes                                           │
│                    │                                            │
│                    ▼                                            │
│    ┌──────────────────────────────────────┐                     │
│    │         MAIN LIST VIEW               │                     │
│    │  ┌──────────────────────────────┐    │                     │
│    │  │ ▸ main         ✓ clean       │    │                     │
│    │  │   feature/auth ● 3 staged    │    │                     │
│    │  │   hotfix/bug   ✗ conflict    │    │                     │
│    │  └──────────────────────────────┘    │                     │
│    │                                      │                     │
│    │  [a]dd [d]el [o]pen [t]erm [f]etch  │                     │
│    └──────────────────────────────────────┘                     │
│         │     │     │      │      │                             │
│         │     │     │      │      └──► git fetch --all          │
│         │     │     │      │                                    │
│         │     │     │      └──► Open terminal at path           │
│         │     │     │                                           │
│         │     │     └──► Open $EDITOR at path                   │
│         │     │                                                 │
│         │     ▼                                                 │
│         │   ┌────────────────────┐                              │
│         │   │  CONFIRM DELETE    │                              │
│         │   │                    │                              │
│         │   │  Delete worktree?  │                              │
│         │   │  [y]es  [n]o       │                              │
│         │   └────────────────────┘                              │
│         │          │                                            │
│         │         Yes ──► git worktree remove                   │
│         │                                                       │
│         ▼                                                       │
│    ┌─────────────────────────────────┐                          │
│    │         ADD MODAL               │                          │
│    │                                 │                          │
│    │  Branch: [feature-auth____]    │                          │
│    │  Base:   main (auto)           │                          │
│    │  Path:   ~/repos/feature-auth  │                          │
│    │                                 │                          │
│    │  [Enter] Create  [Esc] Cancel   │                          │
│    └─────────────────────────────────┘                          │
│              │                                                  │
│            Enter                                                │
│              │                                                  │
│              ▼                                                  │
│    git worktree add -b <branch> <path> <base>                   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 키 바인딩

| Key | Action | Context |
|-----|--------|---------|
| `↑` / `k` | 이전 항목 | Main list |
| `↓` / `j` | 다음 항목 | Main list |
| `a` | Add worktree modal 열기 | Main list |
| `d` | Delete 확인 modal 열기 | Main list |
| `o` | 에디터에서 열기 | Main list |
| `t` | 터미널에서 열기 | Main list |
| `f` | Fetch all | Main list |
| `q` | 종료 | 전체 |
| `Enter` | 확인/실행 | Modal |
| `Esc` | 취소/닫기 | Modal |
| `y` | Yes | Confirm dialog |
| `n` | No | Confirm dialog |

---

## 5. UI 설계

### 5.1 Main View (Worktree List)

```
┌─────────────────────────────────────────────────────────────────────┐
│  gwt v0.1                                    bare: ~/repos/proj.git │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  WORKTREES (3)                                                      │
│  ───────────────────────────────────────────────────────────────    │
│                                                                     │
│  ▸ main              ~/repos/main           ✓ clean      2h ago    │
│    feature/auth      ~/repos/feature-auth   ● 3 staged   5m ago    │
│    hotfix/payment    ~/repos/hotfix-pay     ✗ 2 unstaged just now  │
│                                                                     │
│                                                                     │
│                                                                     │
├─────────────────────────────────────────────────────────────────────┤
│  [a]dd  [d]elete  [o]pen  [t]erminal  [f]etch  [q]uit              │
└─────────────────────────────────────────────────────────────────────┘
```

**상태 아이콘:**

| 아이콘 | 의미 |
|--------|------|
| `✓` | clean (변경 없음) |
| `●` | staged 변경 있음 |
| `✗` | unstaged 변경 있음 |
| `⚡` | conflict 있음 |

### 5.2 Add Worktree Modal

```
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│      ┌───────────────────────────────────────────────────────┐      │
│      │  Add New Worktree                                     │      │
│      ├───────────────────────────────────────────────────────┤      │
│      │                                                       │      │
│      │  Branch name:                                         │      │
│      │  ┌───────────────────────────────────────────────┐    │      │
│      │  │ feature-auth                                  │    │      │
│      │  └───────────────────────────────────────────────┘    │      │
│      │                                                       │      │
│      │  Base branch: main                                    │      │
│      │  Path: ~/repos/feature-auth (auto)                    │      │
│      │                                                       │      │
│      │                    [Enter] Create    [Esc] Cancel     │      │
│      │                                                       │      │
│      └───────────────────────────────────────────────────────┘      │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 5.3 Delete Confirm Modal

```
┌─────────────────────────────────────────────────────────────────────┐
│                                                                     │
│              ┌─────────────────────────────────────────┐            │
│              │                                         │            │
│              │   Delete worktree "feature/auth"?       │            │
│              │                                         │            │
│              │   Path: ~/repos/feature-auth            │            │
│              │   Status: 3 staged changes              │            │
│              │                                         │            │
│              │   ⚠️  This cannot be undone              │            │
│              │                                         │            │
│              │         [y]es          [n]o             │            │
│              │                                         │            │
│              └─────────────────────────────────────────┘            │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 6. 기술 설계

### 6.1 기술 스택

| 영역 | 기술 | 선정 이유 |
|------|------|----------|
| Language | Rust | 단일 바이너리, 성능, TUI 생태계 |
| TUI Framework | ratatui | 성숙한 생태계, 활발한 유지보수 |
| Terminal Backend | crossterm | 크로스플랫폼 지원 |
| Git Operations | Shell commands | MVP 단순화, 기능 완전성 |
| Error Handling | anyhow | 간결한 에러 처리 |

### 6.2 의존성

```toml
[package]
name = "gwt"
version = "0.1.0"
edition = "2021"
description = "Git Worktree TUI Manager"
license = "MIT"

[dependencies]
ratatui = "0.28"
crossterm = "0.28"
anyhow = "1"
```

### 6.3 프로젝트 구조

```
gwt/
├── Cargo.toml
├── README.md
├── src/
│   ├── main.rs              # Entry point, CLI args
│   ├── app.rs               # Application state machine
│   ├── git.rs               # Git shell commands wrapper
│   ├── types.rs             # Worktree, Status 데이터 타입
│   └── ui/
│       ├── mod.rs           # UI module exports
│       ├── main_view.rs     # Worktree list view
│       ├── add_modal.rs     # Add worktree modal
│       └── confirm_modal.rs # Delete confirmation
└── tests/
    └── git_test.rs          # Git operations 테스트
```

### 6.4 핵심 데이터 타입

```rust
// src/types.rs

/// Worktree 정보
pub struct Worktree {
    pub path: PathBuf,
    pub branch: String,
    pub is_bare: bool,
    pub status: WorktreeStatus,
    pub last_commit_time: Option<DateTime<Utc>>,
}

/// Git 상태 요약
pub enum WorktreeStatus {
    Clean,
    Staged(usize),      // staged 파일 수
    Unstaged(usize),    // unstaged 파일 수
    Conflict(usize),    // conflict 파일 수
    Mixed {             // 복합 상태
        staged: usize,
        unstaged: usize,
    },
}

/// Application 상태
pub enum AppState {
    List,
    AddModal { input: String },
    ConfirmDelete { worktree: Worktree },
}
```

### 6.5 Git 명령어 매핑

| 기능 | Shell Command |
|------|---------------|
| List worktrees | `git worktree list --porcelain` |
| Get status | `git -C <path> status --porcelain` |
| Add worktree (새 브랜치) | `git worktree add -b <branch> <path> <base>` |
| Add worktree (기존 브랜치) | `git worktree add <path> <branch>` |
| Remove worktree | `git worktree remove <path>` |
| Fetch all | `git fetch --all` |
| Get last commit | `git -C <path> log -1 --format=%ct` |

---

## 7. 개발 계획

### 7.1 Phase 1: MVP (v0.1) - 2주

```
Week 1: 기반 구조 + Main View
├── Day 1-2: 프로젝트 셋업 & 기반 구조
│   ├── [ ] Cargo 프로젝트 생성
│   ├── [ ] git.rs - worktree list 파싱
│   ├── [ ] git.rs - status 파싱
│   └── [ ] 기본 TUI 이벤트 루프
│
├── Day 3-4: Main View
│   ├── [ ] Worktree 리스트 렌더링
│   ├── [ ] 상태 아이콘 표시
│   ├── [ ] 키보드 네비게이션 (j/k, ↑/↓)
│   └── [ ] 선택 하이라이트
│
└── Day 5: Open 기능
    ├── [ ] Open editor ($EDITOR)
    └── [ ] Open terminal

Week 2: CRUD + 마무리
├── Day 6-7: Add Modal
│   ├── [ ] Modal 렌더링
│   ├── [ ] 텍스트 입력 처리
│   ├── [ ] git worktree add 실행
│   └── [ ] 에러 핸들링
│
├── Day 8-9: Delete & Fetch
│   ├── [ ] Confirm modal
│   ├── [ ] git worktree remove 실행
│   ├── [ ] Fetch all 기능
│   └── [ ] 에러 메시지 표시
│
└── Day 10: 마무리
    ├── [ ] 통합 테스트
    ├── [ ] README 작성
    └── [ ] v0.1 릴리즈
```

**v0.1 완료 기준:**
- [x] bare repo에서 gwt 실행 시 worktree 목록 표시
- [x] 새 worktree 생성 (브랜치명 입력)
- [x] worktree 삭제 (확인 다이얼로그)
- [x] 선택한 worktree에서 에디터/터미널 열기
- [x] 전체 fetch 실행

### 7.2 Phase 2: Bare Repo First (v0.2) - 2주

```
Week 3: Init & Clone
├── Day 1-2: First Run Detection
│   ├── [ ] bare repo 여부 감지
│   ├── [ ] 일반 repo일 때 안내 메시지
│   └── [ ] gwt init 서브커맨드
│
├── Day 3-4: Clone Bare
│   ├── [ ] gwt clone <url> 구현
│   ├── [ ] bare clone + main worktree 생성
│   └── [ ] 진행 상황 표시
│
└── Day 5: Path Convention
    ├── [ ] 기본 경로 규칙 적용
    └── [ ] 브랜치명 → 디렉토리명 변환

Week 4: Polish
├── Day 6-7: Config 기초
│   ├── [ ] ~/.config/gwt/config.toml
│   ├── [ ] 기본 에디터 설정
│   └── [ ] 기본 경로 패턴 설정
│
├── Day 8-9: Branch Cleanup
│   ├── [ ] 삭제 시 브랜치도 삭제할지 옵션
│   └── [ ] 병합된 브랜치 표시
│
└── Day 10: v0.2 릴리즈
    ├── [ ] 문서 업데이트
    └── [ ] 릴리즈 노트
```

**v0.2 완료 기준:**
- [x] `gwt clone <url>` 로 bare repo 셋업 원클릭
- [x] 일반 repo에서 실행 시 bare 전환 가이드
- [x] worktree 경로 자동 생성
- [x] 기본 설정 파일 지원

### 7.3 Phase 3: Project Dashboard (v0.3) - 3주

```
Week 5: Ahead/Behind & Batch
├── [ ] git rev-list로 ahead/behind 계산
├── [ ] 비동기 상태 갱신 (tokio)
├── [ ] Batch fetch 구현
└── [ ] 로딩 인디케이터

Week 6: Split Pane UI
├── [ ] 2-column 레이아웃
├── [ ] Overview 패널
├── [ ] Detail view (staged/unstaged)
└── [ ] 반응형 리사이즈

Week 7: Search & Session
├── [ ] Fuzzy finder (/) 구현
├── [ ] 마지막 선택 worktree 저장
├── [ ] Quick action 단축키
└── [ ] v0.3 릴리즈
```

**v0.3 완료 기준:**
- [x] 모든 worktree의 ahead/behind 한눈에
- [x] Split pane Dashboard UI
- [x] Fuzzy search로 빠른 전환
- [x] Batch fetch + 상태 갱신

### 7.4 Phase 4+: External Integration (v0.4+)

```
Future
├── GitHub/GitLab API 연동
│   ├── [ ] PR 상태 표시
│   └── [ ] CI 상태 표시
│
├── Team Features
│   ├── [ ] .gwt.toml 프로젝트 설정
│   └── [ ] Worktree 템플릿
│
└── Advanced
    ├── [ ] Multiple bare repo 관리
    ├── [ ] Cherry-pick UI
    └── [ ] Stash 관리
```

---

## 8. 설치 및 사용법 (예정)

### 8.1 설치

```bash
# Cargo로 설치
cargo install gwt

# 또는 소스에서 빌드
git clone https://github.com/user/gwt
cd gwt
cargo build --release
```

### 8.2 기본 사용법

```bash
# bare repository 디렉토리에서 실행
cd ~/repos/project.git
gwt

# 또는 경로 지정
gwt --path ~/repos/project.git
```

### 8.3 서브 커맨드 (v0.2+)

```bash
# 새 프로젝트를 bare repo로 클론
gwt clone https://github.com/user/repo.git
# → ~/repos/repo.git (bare)
# → ~/repos/repo/main (첫 번째 worktree)

# 기존 일반 repo를 bare + worktree로 전환
cd ~/existing-repo
gwt init
# → 가이드에 따라 전환

# 특정 경로에 클론
gwt clone https://github.com/user/repo.git --path ~/projects
```

### 8.4 Bare Repository 수동 설정 (참고)

gwt 없이 수동으로 설정하는 방법:

```bash
# 새 프로젝트 시작 시
git clone --bare https://github.com/user/repo.git repo.git
cd repo.git
git worktree add ../main main

# 기존 프로젝트를 bare로 전환
mv .git ../project.git
cd ../project.git
git config --bool core.bare true
git worktree add ../main main
```

### 8.5 권장 디렉토리 구조

```
~/repos/
├── myproject.git/           # bare repository
├── myproject/
│   ├── main/                # main 브랜치 worktree
│   ├── feature-auth/        # feature 브랜치
│   └── hotfix-bug/          # hotfix 브랜치
│
├── another-project.git/
└── another-project/
    ├── main/
    └── develop/
```

---

## 9. 참고 자료

- [Git Worktree 공식 문서](https://git-scm.com/docs/git-worktree)
- [ratatui - Rust TUI Library](https://ratatui.rs/)
- [The Primeagen의 Bare Repo 워크플로우](https://www.youtube.com/watch?v=XXGlHOqIYvE)

---

## Appendix A: MVP State Machine

```
                    ┌─────────────┐
                    │   Loading   │
                    └──────┬──────┘
                           │
              ┌────────────▼────────────┐
              │       Main List         │◄──────────────┐
              └────────────┬────────────┘               │
                           │                           │
        ┌──────────────────┼──────────────────┐        │
        │                  │                  │        │
   Press 'a'          Press 'd'          Press 'o/t/f' │
        │                  │                  │        │
        ▼                  ▼                  │        │
┌───────────────┐  ┌───────────────┐         │        │
│   Add Modal   │  │Confirm Delete │         │        │
└───────┬───────┘  └───────┬───────┘         │        │
        │                  │                  │        │
   Enter/Esc          y/n/Esc                 │        │
        │                  │                  │        │
        └──────────────────┴──────────────────┴────────┘
```

---

## Appendix B: 용어 정의

| 용어 | 설명 |
|------|------|
| Bare Repository | working directory 없이 git 메타데이터만 있는 repository |
| Worktree | 하나의 repository에서 분리된 working directory |
| Base Branch | 새 브랜치를 생성할 때 기준이 되는 브랜치 |
| Ahead/Behind | 현재 브랜치가 remote 대비 앞서거나 뒤처진 커밋 수 |

---

## Appendix C: 경쟁 제품 비교

| 기능 | gwt | lazygit | git-worktree.nvim | git-worktree-switcher |
|------|-----|---------|-------------------|----------------------|
| **Worktree 목록** | ✅ | ✅ (메뉴 깊숙이) | ✅ | ✅ |
| **Worktree 생성** | ✅ | ✅ | ✅ | ❌ |
| **Worktree 삭제** | ✅ | ✅ | ✅ | ❌ |
| **상태 표시** | ✅ | ✅ (개별만) | ❌ | ❌ |
| **Ahead/Behind** | ✅ (v0.3) | ❌ | ❌ | ❌ |
| **Batch Fetch** | ✅ (v0.3) | ❌ | ❌ | ❌ |
| **Bare Repo First** | ✅ (v0.2) | ❌ | ❌ | ❌ |
| **Clone Bare** | ✅ (v0.2) | ❌ | ❌ | ❌ |
| **Dashboard View** | ✅ (v0.3) | ❌ | ❌ | ❌ |
| **Fuzzy Search** | ✅ (v0.3) | ✅ | ✅ | ✅ |
| **에디터 독립** | ✅ | ✅ | ❌ (Neovim) | ✅ |
| **PR 상태** | 🔮 (v0.4) | ❌ | ❌ | ❌ |

**gwt 핵심 차별점:**
1. Bare repo + worktree 패턴을 기본 전제로 설계
2. 모든 worktree 상태를 Dashboard로 한눈에
3. 멀티 브랜치 동시 작업에 최적화
4. AI 코딩 도구 (Claude Code 등) 워크플로우 친화적

---

## Appendix D: 설정 파일 예시 (v0.2+)

```toml
# ~/.config/gwt/config.toml

[core]
# 기본 에디터 (미설정 시 $EDITOR 사용)
editor = "code"

# 기본 터미널
terminal = "ghostty"

[path]
# worktree 경로 패턴
# {project} = bare repo 이름 (확장자 제외)
# {branch} = 브랜치명 (슬래시 → 하이픈)
pattern = "~/{project}/{branch}"

# 예시:
# bare: ~/repos/myapp.git
# branch: feature/auth
# → ~/repos/myapp/feature-auth

[ui]
# 상태 새로고침 간격 (초)
refresh_interval = 30

# 색상 테마 (v0.4+)
# theme = "default"
```

```toml
# 프로젝트별 설정: ~/repos/myapp.git/.gwt.toml (v0.4+)

[path]
# 프로젝트별 경로 패턴 오버라이드
pattern = "~/repos/myapp-{branch}"

[branch]
# 브랜치 템플릿
templates = [
    { prefix = "feature/", base = "develop" },
    { prefix = "hotfix/", base = "main" },
    { prefix = "release/", base = "develop" },
]
```

---

*문서 버전: v0.2-draft*  
*최종 수정: 2026-01-24*
